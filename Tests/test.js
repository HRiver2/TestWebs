var a = 10;
let b = 114514 ** 10; // 114514 的 10 次方
const MXX = 0x7fffffff;
console.log(a); // 输出 a
alart('Hello World'); // 以弹出窗口方式输出 Hello World
alart(aString); // var 可以被声明之前调用（但是不会存在值，即处于 undefined 状态    ）
var aString = 'Deltana'
let unknowT = undefined; // 表示⼀个变量已声明但未赋值

let arr = [1, 2, 3, 4, 6];
let obj = {name: "Intergalatic", value: 40};
console.log(obj.name);

let wtf = [
    {
        man : "what can i say",
        manba : "out"
    }, {
        
    }
]

function TheC45(a, b, c, d) {
    alart(a), alart(b), alart(c);

    () => {
        let stupid = true;
    } // 匿名函数

    if(a === x) {

    } // === 为严格相等

    for(var i = 1; i <= a; i += 1) {

    }

    // return (0 - 0);

    console.log(1);
    setTimeout("console.log(2)", 1000);
    setInterval("console.log(3)", 1000);
}

const std = document.getElementById("intro");
const std2 = document.getElementsByClassName("para");
const testify = document.querySelector("vivid");
const bpx = document.getElementsByClassName("box");

bpx.addEventListener('click', function(){alart("爱丽丝错了爱丽丝不该在网上口嗨的");}, 1);